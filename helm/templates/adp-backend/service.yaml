apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.adpBackend.appName }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - protocol: TCP
      name: web
      port: {{ .Values.adpBackend.containerPort }}   
  selector:
    app: {{ .Values.adpBackend.appName }}