apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.mysql.appName }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.mysql.appName }} 
  serviceName: {{ .Values.mysql.appName }}
  replicas: {{ .Values.replicas }}
  template:
    metadata:
      labels:
        app: {{ .Values.mysql.appName }} # has to match .spec.selector.matchLabels
    spec:
      terminationGracePeriodSeconds: 10
      initContainers:
      - name: init-sysctl
        image: busybox
        imagePullPolicy: IfNotPresent
        command: ["/bin/sh","-c"]
        args: ["sysctl -w vm.max_map_count=262144; chown -R 1000:1000 {{ .Values.mysql.volumeMountsPath }}"]
        securityContext:
          privileged: true
        volumeMounts:
          - name: {{ .Values.mysql.volumeMountsName }}
            mountPath: {{ .Values.mysql.volumeMountsPath }}
      containers:
      - name: {{ .Values.mysql.appName }}
        image: {{ .Values.mysql.image }}
        args:
          - --character-set-server=utf8
          - --innodb_log_buffer_size=800M
          - --innodb_log_file_size=80M
          - --max_allowed_packet=8M 
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "simple"
        - name: MYSQL_DATABASE
          value: "adp"
        ports:
        - containerPort: {{ .Values.mysql.containerPort }}
          name: {{ .Values.mysql.appName }}
        volumeMounts:
        - name: {{ .Values.mysql.volumeMountsName }}
          mountPath: {{ .Values.mysql.volumeMountsPath }}
  volumeClaimTemplates:
  - metadata:
      name: {{ .Values.mysql.volumeMountsName }}
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 5Gi

